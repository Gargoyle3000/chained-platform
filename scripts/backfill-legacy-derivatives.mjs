import { readFileSync } from "node:fs";
import { spawnSync } from "node:child_process";
import { trustedImageDimensions } from "../supabase/functions/_shared/work-media.ts";
import { parseBackfillArguments } from "./backfill-legacy-derivatives-options.mjs";
const api="https://jjtobvxjmbnybbxlvnxs.supabase.co", fail=(x)=>{throw Error(x)};
const { apply, all, work, ids } = parseBackfillArguments(process.argv.slice(2));
const key=(readFileSync("tests/.local/production-cloud-run-smoke.env","utf8").match(/^CHAINED_PRODUCTION_SUPABASE_SECRET_KEY=(.+)$/m)||[])[1]; if(process.env.CHAINED_PRODUCTION_SMOKE!=="1"||!key) fail("production_guard_failed");
const sql=(q)=>{const r=spawnSync("powershell.exe",["-NoProfile","-NonInteractive","-Command",`npx.cmd supabase db query --linked --output json \"${q.replaceAll('"','\\"')}\"`],{encoding:"utf8",env:{...process.env,SUPABASE_TELEMETRY_DISABLED:"1"}});if(r.status)fail("database_read_failed");return JSON.parse(r.stdout.slice(r.stdout.indexOf("{"))).rows};
const filter=ids.length?`wi.id in (${ids.map(x=>`'${x}'::uuid`).join(',')})`:work?`wi.work_id='${work}'::uuid`:`wi.deleted_at is null`;
const rows=sql(`select wi.id::text image_id,wi.work_id::text work_id,w.visibility::text visibility,wi.private_object_path,wi.pixel_width,wi.pixel_height from public.work_images wi join public.works w on w.id=wi.work_id where ${filter} and wi.deleted_at is null and w.deleted_at is null and wi.upload_status='ready' and wi.original_verified_at is not null and not exists(select 1 from private.work_image_derivative_jobs j where j.work_image_id=wi.id and j.source_private_object_path=wi.private_object_path) and not exists(select 1 from private.work_image_derivatives d where d.work_image_id=wi.id and d.source_private_object_path=wi.private_object_path)`);
const results=[];for(const row of rows){const path=row.private_object_path.split('/').map(encodeURIComponent).join('/');const r=await fetch(`${api}/storage/v1/object/authenticated/work-originals/${path}`,{headers:{apikey:key}});if(!r.ok)fail("original_read_failed");const d=trustedImageDimensions(new Uint8Array(await r.arrayBuffer()),(r.headers.get("content-type")||"").split(';',1)[0]);if(!d)fail("trusted_dimensions_unavailable");if(apply){const x=await fetch(`${api}/rest/v1/rpc/service_backfill_legacy_work_image_derivatives`,{method:"POST",headers:{apikey:key,"content-type":"application/json"},body:JSON.stringify({target_image_id:row.image_id,authoritative_width:d.width,authoritative_height:d.height})});if(!x.ok)fail("backfill_rpc_failed")}results.push({work_id:row.work_id,image_id:row.image_id,visibility:row.visibility,stored_dimensions:{width:row.pixel_width,height:row.pixel_height},authoritative_dimensions:d,eligible:true,action:apply?"backfilled":"would_backfill"})};console.log(JSON.stringify({mode:apply?"apply":"dry_run",results}));
